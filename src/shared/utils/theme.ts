import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#00939f',
      contrastText: '#000000',
    },
    secondary: {
      main: '#000000',
      contrastText: '#FFFFFF',
    },
  },
});